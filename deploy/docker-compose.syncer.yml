# æ•°æ®åŒæ­¥æœåŠ¡
# ä»?api.modrinth.com åŒæ­¥é¡¹ç›®/ç‰ˆæœ¬åˆ°æœ¬åœ°å®žä¾?#
# ç”¨æ³•:
#   docker compose -f docker-compose.yml -f docker-compose.syncer.yml run --rm syncer
#   docker compose -f docker-compose.yml -f docker-compose.syncer.yml run --rm syncer --limit 200 --project-type modpack

services:
  syncer:
    build:
      context: ./sync
      dockerfile: Dockerfile
    container_name: 8h8g-syncer
    volumes:
      - /data/sync:/data/sync
    environment:
      TARGET_API: http://labrinth:8000/v2
      TARGET_ADMIN_KEY: ${LABRINTH_ADMIN_KEY:-}
      SYNC_DIR: /data/sync
    networks:
      - 8h8g_default
    profiles:
      - sync

volumes:
  sync-data:

networks:
  # ä½¿ç”¨ä¸?compose æ–‡ä»¶çš„é»˜è®¤ç½‘ç»œï¼Œè®?syncer èƒ½è®¿é—?labrinth
  default:
    name: 8h8g_default
    external: true
